/** Price in paise (500 = ₹5) — keep in sync with /api/generate-image */
export const AI_IMAGE_GENERATION_PRICE_PAISE = 500

/** Max prompt length for Fal / abuse prevention */
export const AI_IMAGE_PROMPT_MAX_LENGTH = 2000
