/** Shared marketing copy for streamer → studio upgrade (whitelabel / custom domain). */

/** Full text for the Upgrade to Studio checkout dialog (`StudioUpgradeCheckoutDialog`). */
export const STUDIO_UPGRADE_CHECKOUT_DIALOG_DESCRIPTION =
  "Step 1: pay for your annual subscription here (tax follows platform settings). Step 2: after payment succeeds, you are redirected to the Studio setup wizard—company details, branding, domain, and preferences."

/** Shown on the streamer upgrade card so users know the wizard is not this modal. */
export const STUDIO_UPGRADE_CALLOUT_FLOW_HINT =
  "This opens checkout first. The Studio setup wizard (branding and domain) starts after your payment succeeds."

export const STUDIO_UPGRADE_DOMAIN_HINT =
  "Typically you point your root domain with an A record or your subdomain with a CNAME to the platform target your administrator provides (often configured via NEXT_PUBLIC_PLATFORM_A_RECORD_IP and NEXT_PUBLIC_PLATFORM_CNAME_TARGET). Our team guides you through verification after upgrade."
